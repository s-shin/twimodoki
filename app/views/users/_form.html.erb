<%= form_for(@user, html: {class: "form-horizontal"}) do |f| %>
  <% if @user.errors.any? %>
    <div class="errors">
  		<h4><%= @user.errors.count %> 個の入力エラーがあります。</h4>
      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="control-group">
    <%= f.label :another_name, class: "control-label" %>
		<div class="controls">
			<%= f.text_field :another_name, required: "required" %>
		</div>
  </div>
  <div class="control-group">
    <%= f.label :name, class: "control-label" %>
		<div class="controls username">
			<%= f.text_field :name, required: "required" %>
			<p class="help-block">あなたのURL: <%= root_url %><span>...</span></p>
		</div>
  </div>
  <div class="control-group">
    <%= f.label :password, class: "control-label" %>
		<div class="controls">
			<%= f.password_field :password, required: "required" %>
		</div>
  </div>
  <div class="control-group">
    <%= f.label :password_confirmation, class: "control-label" %>
		<div class="controls">
	    <%= f.password_field :password_confirmation, required: "required" %>
		</div>
  </div>
  <div class="control-group">
    <%= f.label :email, class: "control-label" %>
		<div class="controls">
	    <%= f.email_field :email, required: "required" %>
		</div>
  </div>
  <div class="control-group">
		<div class="controls">
			<label class="checkbox">
				<%= f.check_box :private %>つぶやきを非公開にする
			</label>
		</div>
  </div>
  <div class="control-group">
		<div class="controls">
		  <%= f.submit "アカウントを作成する", class: "btn" %>
		</div>
  </div>
<% end %>
